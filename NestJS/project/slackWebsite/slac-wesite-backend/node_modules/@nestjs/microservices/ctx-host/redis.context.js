import { BaseRpcContext } from './base-rpc.context.js';
/**
 * @publicApi
 */
export class RedisContext extends BaseRpcContext {
    constructor(args) {
        super(args);
    }
    /**
     * Returns the name of the channel.
     */
    getChannel() {
        return this.args[0];
    }
}
