export class ClientsContainer {
    clients = [];
    getAllClients() {
        return this.clients;
    }
    addClient(client) {
        this.clients.push(client);
    }
    clear() {
        this.clients = [];
    }
}
