
project rule:
user and workspace has many to many relatinshp
workspace has 1 to many with message
workspace has 1 to maany with channel
channel has 1 to many with message
user has one to many with message
user can exists whether workspace exists or not
but chnannel exists if workspace exists

when we create workspace then we add member(owner/admin) to workspace and also we need to create channel.




If your rule is:
A channel name must be unique inside a workspace, but the same name can exist in different workspaces
then your database should ideally have a composite unique constraint:

(workspace_id, name)

Then:

Workspace 1 → general ✅
Workspace 1 → general ❌
Workspace 2 → general ✅

Your application check is useful, but the database constraint protects you from race conditions too.

Your entity design itself is correct for Workspace → Channels.

if there is something which can exists then for defining relationship bw them we create new entity; like workspace-user
but if something exist if only other thing exist then we define relatinshp directly like workspace->chnannel

note->
common response will be handled by interceptor
common response should be in application level


-->forRoot()       → direct configuration
forRootAsync()  → configuration using ConfigService / factory / async logic

-->
export const dbConfig = (
  configService: ConfigService,
): TypeOrmModuleOptions ()=>{}
you tell TypeScript:
"This function must return a valid TypeORM configuration

-->
Option 1 — @BeforeInsert() / @BeforeUpdate() ⭐
For your case, this is the simplest approach.

import {
  Entity,
  PrimaryGeneratedColumn,
  Column,
  BeforeInsert,
  BeforeUpdate,
} from 'typeorm';

@Entity('users')
export class User {
  @PrimaryGeneratedColumn()
  id: number;

  @Column({ unique: true })
  email: string;

  @Column()
  password: string;

  @Column({ unique: true })
  username: string;

  @Column({ nullable: true })
  avatar: string;

  @BeforeInsert()
  @BeforeUpdate()
  generateAvatar() {
    this.avatar = `https://robohash.org/${this.username}`;
  }
}
What happens?
When you do:
await userRepository.save(user);

TypeORM does:

save(user)
   ↓
@BeforeInsert()
   ↓
generateAvatar()
   ↓
avatar = https://robohash.org/naeem
   ↓
INSERT into users

For an existing user:

save(user)
   ↓
@BeforeUpdate()
   ↓
generateAvatar()
   ↓
UPDATE users
But there is an important difference

Mongoose:
pre('save')
is a middleware hook.

TypeORM:
@BeforeInsert()
@BeforeUpdate()
are entity lifecycle hooks/listeners.

Option 2 — Subscriber
For more complex logic, use a TypeORM subscriber:
@EventSubscriber()
export class UserSubscriber
  implements EntitySubscriberInterface<User> {
  listenTo() {
    return User;
  }
  beforeInsert(event: InsertEvent<User>) {
    event.entity.avatar =
      `https://robohash.org/${event.entity.username}`;
  }
}
Use a subscriber when the logic is more complicated or you want to keep the Entity clean.
For your project
For this simple avatar generation:

Mongoose:
pre('save')

       ↓ equivalent idea ↓

TypeORM:
@BeforeInsert()
@BeforeUpdate()

I'd use @BeforeInsert() if the avatar should only be generated when creating the user, 
or both decorators if it should change whenever the username changes.
note->
@BeforeInsert() does NOT run with:
repository.insert()
same for Mongoose because
insert() is a direct database operation. It bypasses entity lifecycle hooks.
then put login at the time of insertion

save()   → lifecycle hooks ✅
insert() → lifecycle hooks ❌
update() → lifecycle hooks ❌

--->

ValidationPipe
│
├── whitelist
│      → remove unknown fields
│
├── forbidNonWhitelisted
│      → reject unknown fields
│
└── transform
       → convert input to DTO types


--->
You should throw a GraphQL exception instead of a NestJS HTTP exception:
syntax:throw new GraphQLError(message,{
  extensions:{
    code:metadata
  }
})


import { GraphQLError } from 'graphql';

throw new GraphQLError('Email or username already exists', {
  extensions: {
    code: 'CONFLICT',
    statusCode:409
  },
});

//extensions is operational

Yes. The key is understanding that HTTP and GraphQL have different error models.
1. NestJS HTTP exception
When you write:

throw new ConflictException('Email or username already exists');
ConflictException is an HTTP-oriented exception.

It represents:
HTTP 409 Conflict

This makes perfect sense in a REST API:

POST /users
        ↓
duplicate email
        ↓
HTTP 409

The response might be:
{
  "statusCode": 409,
  "message": "Email or username already exists"
}


2. GraphQL works differently
GraphQL normally returns one HTTP endpoint:

POST /graphql
Even if your GraphQL operation has a business error, the HTTP request itself can still be successfully processed.
GraphQL puts the error inside the GraphQL response:
{
  "errors": [
    {
      "message": "Email or username already exists",
      "extensions": {
        "code": "CONFLICT"
      }
    }
  ],
  "data": null
}

So:

HTTP
└── status code → 200
    GraphQL
    └── errors[].extensions.code → CONFLICT

That's the fundamental difference.
How GraphQLError works

You write:

import { GraphQLError } from 'graphql';

throw new GraphQLError(
  'Email or username already exists',
  {
    extensions: {
      code: 'CONFLICT',
    },
  },
);

GraphQLError is an error class from the GraphQL library.
The important part is:

extensions: {
  code: 'CONFLICT',
}

You're adding metadata to the GraphQL error.
Apollo then sends that information to the client:

{
  "errors": [
    {
      "message": "Email or username already exists",
      "extensions": {
        "code": "CONFLICT"
      }
    }
  ]
}
Why did your ConflictException become INTERNAL_SERVER_ERROR?

You had:

throw new ConflictException(
  'Email or username already exists',
);

NestJS knows:

ConflictException
       ↓
HTTP 409

But Apollo doesn't treat NestJS's ConflictException as one of its GraphQL error codes.
So Apollo saw an unrecognized exception and produced:

"extensions": {
  "code": "INTERNAL_SERVER_ERROR"
}

Even though the underlying exception contained:
"statusCode": 409

That's why your previous response had both:

code: INTERNAL_SERVER_ERROR
status: 409

Can you use HTTP exceptions with GraphQL?

Yes, absolutely.
NestJS allows you to use:
BadRequestException
UnauthorizedException
ForbiddenException
NotFoundException
ConflictException

inside GraphQL resolvers/services.
They won't crash the application.

The question is how you want your GraphQL API to expose those errors.

Option 1 — NestJS exceptions
throw new ConflictException('Email already exists');

Simple and familiar if you're already using NestJS.

Option 2 — GraphQL errors
throw new GraphQLError('Email already exists', {
  extensions: {
    code: 'CONFLICT',
  },
});

More naturally aligned with GraphQL's error model.

One important correction
You don't necessarily need to replace every NestJS exception with GraphQLError.

For example, your validation:
new ValidationPipe()

is already producing a BadRequestException, and NestJS/GraphQL is handling that for you.

So you can choose a consistent strategy rather than thinking:
"HTTP exceptions cannot be used in GraphQL."

They can be used. It's just that GraphQL has its own error-response format.

---->
Think of the HTTP layer and GraphQL layer separately.

Request
When a GraphQL query/mutation is sent:

query {
  users {
    id
    name
  }
}

The HTTP request body is roughly:

{
  "query": "query { users { id name } }",
  "variables": {}
}

for mutation:
{
  "query": "mutation { users { id name } }",
  "variables": {}
}

So:
req.body
contains the GraphQL request payload (query, variables, etc.).

➡️ contains the GraphQL query/mutation request payload.
But req.body is not the clean mutation input like Express REST.

--->
Response
Suppose your resolver is:

@Query(() => [User])
users() {
  return [
    { id: 1, name: "Naeem" },
    { id: 2, name: "John" }
  ];
}

Then next.handle() in your interceptor receives the resolver's result:

data = [
  { id: 1, name: "Naeem" },
  { id: 2, name: "John" }
];

If your interceptor does:

map((data) => ({
  success: true,
  message: 'Request successful',
  data,
}))

then GraphQL will use that transformed value as the resolver result.
Conceptually, the final GraphQL response becomes:

{
  "data": {
    "users": {
      "success": true,
      "message": "Request successful",
      "data": [
        {
          "id": 1,
          "name": "Naeem"
        },
        {
          "id": 2,
          "name": "John"
        }
      ]
    }
  }
}

But: your GraphQL schema(means you query and mutation objectType) must allow that response shape. 
You can't arbitrarily add success, message, and data around a resolver that is declared to return [User].

Remember this flow
HTTP Request
     ↓
req.body

{ query, variables }
     ↓
GraphQL
     ↓
Resolver
     ↓
next.handle()
     ↓
data = resolver's return value
     ↓
Interceptor map()
     ↓
     resolver
     ↓
GraphQL Response
{ data: ... }


So req.body = GraphQL request, 
while next.handle() → data = resolver result, and GraphQL then builds the final response around it.

and:
res is the HTTP response object.
It does not contain the resolver's returned GraphQL data in the way next.handle() does.
in express it used to response data.

The easiest way to remember                                    
When React uses Apollo Client's useQuery() or useMutation(),or client.query() ,client.mutate() the flow is roughly:
or 
postman

React/
  │
  │ useQuery() / useMutation()
  ▼
Apollo Client
  │
  │ HTTP POST /graphql
  │
  │ {
  │   query: "...",
  │   variables: {...}
  │ }
  ▼
GraphQL Server (NestJS)
  │
  ▼
GraphQL Module
  │
  ▼
Interceptor / Guards / Pipes
  │
  ▼
Resolver
  │
  ▼
Service
  │
  ▼
Database

Then the result comes back:

Database
   ↓
Service
   ↓
Resolver
   ↓
Interceptor
   ↓
GraphQL Server
   ↓
HTTP Response
   ↓
Apollo Client
   ↓
React Component


this is the flow for every language 
Example

React:

const { data } = useQuery(GET_USERS);
Apollo sends an HTTP request like:

POST /graphql
Content-Type: application/json

with a body conceptually like:

{
  "query": "query GetUsers { users { id name } }",
  "variables": {}
}

because : localhost:3000/graphql now got the idea

NestJS receives it:
req.body

contains the GraphQL operation.
Then GraphQL figures out:

"This is the users query, so I need to call the users() resolver."

Your resolver:

@Query(() => [User])
users() {
  return this.userService.getUsers();
}

The returned value goes through:

next.handle()
in your interceptor.

Then your interceptor can transform it:

map((data) => ({
  success: true,
  message: 'Users fetched',
  data,
}))

Finally, GraphQL sends the HTTP response back to Apollo Client.

One important distinction
useQuery() / useMutation() are React-side APIs. They don't directly call your resolver.

They trigger Apollo Client → HTTP request → GraphQL server → Resolver.

So your mental model should be:

React useQuery/useMutation → Apollo Client → HTTP GraphQL request → 
NestJS GraphQL server → Resolver → Service → DB → Response → Apollo → React.


---->
if you want to know which resolver method/class produced the data, 
ExecutionContext can give you the handler (method) and class.
context.getHandler()  // method
context.getClass()    // class

For names:

context.getHandler().name  // getUsers
context.getClass().name    // UserResolver

--->
These are common properties you may see on JavaScript/Node.js errors, but not every error has all of them.
1. error.code
Identifies the type/category of error.

Example with MySQL:
error.code === 'ER_DUP_ENTRY'
Means: duplicate value — e.g. trying to insert an email that already exists.

Other examples:

ER_DUP_ENTRY
ER_NO_SUCH_TABLE
ER_ACCESS_DENIED_ERROR

Think: "What kind of error is this?"

2. error.message
Contains the human-readable explanation of the error.

Example:
error.message

Could return:
Duplicate entry 'test@gmail.com' for key 'users.email'
Think: "What exactly went wrong?"

3. error.name
Contains the name/type of the JavaScript error.
Example:
error.name
Could return:

Error
TypeError
ReferenceError
SyntaxError

Example:

try {
  null.someMethod();
} catch (error) {
  console.log(error.name);
}

Output:
TypeError

Think: "What JavaScript error class is this?"

4. error.status
Usually represents an HTTP status code, but it is not guaranteed to exist.

Example:
error.status

Could be:

400
404
500

For example:

throw {
  status: 404,
  message: 'User not found'
};

Think: "What HTTP status should I return?"

5. error.statusCode
Same general idea as status, but commonly used to explicitly mean an HTTP status code.

Example:
error.statusCode

Could be:
400
401
404
500

In NestJS, you'll commonly encounter exceptions such as:

throw new NotFoundException('User not found');
The exception has an HTTP status of:
404
Think: "What HTTP status code should the client receive?"

note->
Some internal/programming errors do not have statusCode at all.
For example:

try {
  const user = await User.findById(id);
} catch (error) {
  console.log(error.statusCode);
}

If the database throws an error, it might look like:

{
  code: "ER_DUP_ENTRY",
  message: "Duplicate entry...",
  name: "Error"
}

There may be no:

statusCode
status

So:

error.statusCode // undefined
Common examples

1. Database error

error.code = "ER_DUP_ENTRY"
error.message = "Duplicate entry..."

❌ statusCode may not exist.

2. Network error

error.code = "ECONNREFUSED"
error.message = "Connection refused"

❌ statusCode may not exist.

3. JavaScript error

error.name = "TypeError"
error.message = "Cannot read properties of undefined"

❌ statusCode usually doesn't exist.

4. NestJS HTTP exception

throw new NotFoundException("User not found");

✅ Has HTTP status 404.

Important concept
Database/Node error
       ↓
may NOT have statusCode
       ↓
your application should assign one
       ↓
500 Internal Server Error

For example:
const statusCode = error.statusCode || 500;

Meaning:
If the error has a statusCode, use it. Otherwise, use 500.

---->
If you want to modify the GraphQL error response
This is where your interceptor can be useful.

For example, you might want the client to receive:

{
  "success": false,
  "message": "Email or username already exists",
  "code": "CONFLICT",
  "statusCode": 409
}

However, there's an important distinction:
NestJS interceptors are mainly for successful resolver responses.
 GraphQL exceptions are handled by GraphQL's exception/error pipeline, 
 so an interceptor does not modify graphql response

For GraphQL, use a formatError function in GraphQLModule:
For GraphQL, use a formatError function in GraphQLModule:

GraphQLModule.forRoot({
  driver: ApolloDriver,

  formatError: (error) => {
  return {
    
    message: error.message,
    code: error.extensions?.code,
    statusCode: error.extensions?.statusCode, 
    path: error.path,
  };
},
});

Then your client can receive something like:

{
  "errors": [
    {
      "message": "Email or username already exists",      '
      "code": "CONFLICT",
      "statusCode": 409
      path:'singup'
    }
  ]
}


status code will be shown if you have put it in GraphQLError extensions 
when we throw graphql and put any property in extensions then that properties it add to and error 
When you throw a GraphQLError, it contains standard properties such as:

GraphQLError
├── message
├── locations
├── path
└── extensions

extensions is used for additional/custom information.
extensions: {
  code: 'INVALID_PASSWORD',
  statusCode: 401
}

Whatever properties you put inside extensions become available under:
error.extensions

So:

extensions
├── code
└── statusCode
└── originalError
stacktrace — stacktrace tells you where and how an error happened inside your code/application.



originalError in side extensions
originalError means the underlying/original error from which a GraphQL error was created or wrapped.

For example:

NestJS ValidationPipe
        ↓
BadRequestException
        ↓
GraphQL wraps the exception
        ↓
GraphQL error

The original NestJS exception can be available as:

error.extensions.originalError

Conceptually:

GraphQL Error
├── message
├── path
├── locations
└── extensions
    ├── code
    └── originalError
        ├── message
        ├── statusCode
        └── other original 
        
In one line
extensions = extra information you attach to a GraphQL error.
originalError = the underlying error that was wrapped into/associated with the GraphQL error.
And originalError is not guaranteed to exist on every GraphQLError.


--->
| Registration             | Purpose                                                          |
| ------------------------ | ---------------------------------------------------------------- |
| `forRoot()`              | Connect TypeORM + know entities                                  |
| `forFeature([Entity])`   | Make repository available in that module                         |
| `autoLoadEntities: true` | Automatically collect entities registered through `forFeature()` |
if use autoLoadEntities:true then we don't need to registered entities in entities field insdie forRoot()

--->
Option 1 — Using the relation
You give TypeORM the User object through the relation property, 
and TypeORM extracts the user's ID and stores that ID in the FK column.
  const post = postRepository.create({
    title: 'My Post',
    user: user, // User entity object
  });

await postRepository.save(post);

TypeORM will automatically put that user's ID into the FK column:

posts
----------------
id
title
userId  ← automatically stored

or
const user = await userRepository.findOneBy({ id: 5 });

const post = postRepository.create({
  title: 'Hello',
  user: user,   // whole User object here
});

await postRepository.save(post);


user means a User entity object,
 which normally contains the user's primary key (id) along with its other properties.
 if User entity doesn't have an id property, then TypeORM cannot use user.id specifically. 
 It will use whatever property is defined as the primary key.

 For example:

@Entity()
export class User {
  @PrimaryGeneratedColumn()
  userId: number;

  @Column()
  name: string;
}

Here the primary key is:
userId
It cares about which property has @PrimaryColumn() / @PrimaryGeneratedColumn().

Option 2 — Manually using userId
You can also explicitly define the FK column:

@ManyToOne(() => User, (user) => user.posts)
@JoinColumn({ name: 'userId' })
user: User;

@Column()
userId: number;

Then:

const post = postRepository.create({
  title: 'My Post',
  userId: 5,
});

In a real NestJS API
Usually, you don't take userId from the frontend for an authenticated post.

Instead:
const user = req.user; // user obtained from JWT
const post = this.postRepository.create({
  title: input.title,
  user: user,
});

await this.postRepository.save(post);

So the flow is:

JWT
 ↓
identify User
 ↓
create Post
 ↓
post.user = User
 ↓
TypeORM stores user.id in posts.userId

Key point: The FK value has to be provided somehow, but TypeORM can populate the FK from the related User entity for you.

note->
we delacre user(parent) object only foreign key (child)side.
we do not have to assign  on both sides.
in child relation side we can optain parent data by relations or parent relation side we can optain child data by relations field.
relatinshp attribute auto included in these respective object so we can apply filter according to that.
but if we want to retrieve that then we should use relations;

example:
--->
Find a channel whose name is channelName AND whose workspace's id equals workspaceId.

where: {
  name: channelName,
  workspace: {
    id: workspaceId,
  },
}

here workspace is workspace entity data as defined in relatinshp
Think of it as SQL:

SELECT *
FROM channel
WHERE name = 'general'
AND workspace_id = 5;


why this works
This part:

@ManyToOne(
  () => WorkspaceEntity,
  (workspace) => workspace.channels,
  { onDelete: 'CASCADE' },
)
@JoinColumn({ name: 'workspace_id' })
workspace: Relation<WorkspaceEntity>;

means the channels table will have:

channels
--------------------------------
id
name
createdAt
updatedAt
workspace_id   ← FK

So this query:

const existingChannel = await this.channelRepository.findOne({
  where: {
    name: channelName,
    workspace: {
      id: workspaceId,
    },
  },
});

means:
Find a channel with this name where its workspace has this ID.

Why { id: workspaceId } works

Your workspace property is:
workspace: Relation<WorkspaceEntity>;

So conceptually:
channel.workspace
is a WorkspaceEntity.

And a WorkspaceEntity has:
workspace.id

Therefore:

workspace: {
  id: workspaceId,
}

is saying:

channel.workspace.id === workspaceId
One important improvement



notes->
In TypeORM, true is commonly used when you want to say "yes, include/load this relation", not "filter by this relation."
1. relations: { workspace: true }
Used when loading related data:

const channel = await this.channelRepository.findOne({
  where: { id: 1 },
  relations: {
    workspace: true,
  },
});

Meaning:
Find channel 1 and also load its workspace.
Without it, depending on your relation configuration, you may get:

channel.id
channel.name
channel.workspace // not loaded

With:

relations: {
  workspace: true,
}

you get:

channel.workspace.id
channel.workspace.name
2. where → use actual values

For filtering:

where: {
  workspace: {
    id: workspaceId,
  },
}

Meaning:
Find channels whose workspace ID equals workspaceId.

Not:
workspace: true ❌
Easy rule
true → LOAD the relation
value → FILTER by the relation

Examples:

// LOAD
relations: {
  workspace: true,
}
// FILTER
where: {
  workspace: {
    id: 5,
  },
}

So true is about loading/including a relation, while id: workspaceId is about searching/filtering by that relation.
so because of relationship entity data  is auto including both side and can apply filter but for  laoding/returing we need to say relations:{name:true}

--->
const users = await this.userRepository.find({
  select: {
    id: true,
    name: true,
    email: true,
  },
});
true  → include column
omit  → don't include column

and
const user = await this.userRepository
  .createQueryBuilder('user')
  .select(['user.id', 'user.name', 'user.email'])
  .where('user.id = :id', { id })
  .getOne();


--->
const workspaces = await this.workSpaceRepo
  .createQueryBuilder('workspace')
  .innerJoin('workspace.members', 'member')
  .where('member.user_id = :memberId', { memberId })
  .getMany();

  there is one to many relatinshp bw workspace and workspaceMember
  workspace has members(list of member) field as defined in relationship so this will perform innerJoin with workspace.members
  with where conditions

note->

The members do temporarily form one JOIN result, but they do NOT lose their workspace relationship.

Suppose:

Workspace table
workspace
----------------
id | name
1  | A
2  | B
3  | C
Member table
member
---------------------------
id | workspace_id | user_id
1  | 1            | 10
2  | 1            | 20
3  | 2            | 10
4  | 2            | 30
5  | 3            | 40

Each workspace has its own members:

Workspace A → [member 1, member 2]
Workspace B → [member 3, member 4]
Workspace C → [member 5]

What does JOIN do?
When you write:

.innerJoin('workspace.members', 'member')
SQL creates a JOIN result roughly like:

workspace   member
---------   ------
A           member 1
A           member 2
B           member 3
B           member 4
C           member 5

So yes, you can think of it as one combined intermediate result:

A → member 1
A → member 2
B → member 3
B → member 4
C → member 5

But notice:
The relationship is still preserved in every row through the workspace columns.

It doesn't become:
members = [1,2,3,4,5]
without knowing which workspace they belong to.
--->
innerJoin()
JOINs the relation, but does NOT return the joined data.

.createQueryBuilder('workspace')
.innerJoin('workspace.members', 'member')
.where('member.user_id = :memberId', { memberId })
.getMany();

Think:

Workspace
    ↓ JOIN
Members
    ↓
use members for WHERE/filter
    ↓
return only Workspace

Result:

[
  {
    id: 1,
    name: 'Workspace A'
    // members not loaded
  }
]


innerJoinAndSelect()
JOINs the relation AND includes the joined data in the result.

.createQueryBuilder('workspace')
.innerJoinAndSelect('workspace.members', 'member')
.where('member.user_id = :memberId', { memberId })
.getMany();

Think:

Workspace
    ↓ JOIN
Members
    ↓
use members for WHERE/filter
    ↓
return Workspace + Members

Result:

[
  {
    id: 1,
    name: 'Workspace A',
    members: [
      { user_id: 10 },
      { user_id: 20 }
    ]
  }
]

--->
| Method         | Returns                 | Use when                                  |
| -------------- | ----------------------- | ----------------------------------------- |
| `getOne()`     | One entity or `null`    | You expect a single row                   |
| `getMany()`    | Array of entities       | You expect multiple rows                  |
| `getRawOne()`  | One raw DB object       | You want selected SQL columns             |
| `getRawMany()` | Array of raw DB objects | You want multiple raw rows                |
| `getCount()`   | Number                  | You only need the count                   |
| `getExists()`  | `boolean`               | You only need to know if something exists |


--->
Because they are two different things in TypeORM.

@ManyToOne(() => User)
@JoinColumn({ name: 'user_id' })
user: User;

1. user is your entity property
user: User;

This means:
In JavaScript/TypeScript, I access the related user through member.user.

Example:

member.user
member.user.id
member.user.name

2. @JoinColumn is a TypeORM decorator
@JoinColumn({ name: 'user_id' })

It tells TypeORM:
The foreign-key column in the database is called user_id.

So:

TypeScript                  Database
-----------                 --------
member.user       ────────> user_id
     │                         │
   User entity              FK column



In your entity
@ManyToOne(() => UserEntity)
@JoinColumn({ name: 'user_id' })
user: Relation<UserEntity>;

You access:

member.user

not:
member.user_id
And normally you don't access member.user_id unless you explicitly define user_id as a separate column/property.

user_id is available in the database,
 but TypeORM does not automatically make every database column a property on your TypeScript entity.

Think of it as two separate layers:

DATABASE                         TYPESCRIPT ENTITY

workspace_members
┌──────────────┐                ┌────────────────────┐
│ id           │                │ id                 │
│ user_id      │ ────────────── │ user               │
│ workspace_id │                │ workspace           │
│ role         │                │ role               │
└──────────────┘  
              └────────────────────┘
Simple rule
user = property you use in TypeScript
@JoinColumn = tells TypeORM how that property maps to the database FK column.

Why can't you do this?
member.user_id

Because your TypeScript class does not have a property called user_id.

You only declared:

user: Relation<UserEntity>;

TypeScript only knows about properties you declare on the class.

If you WANT member.user_id
You can explicitly map it:

@Column({ name: 'user_id' })
userId: number;

@ManyToOne(() => UserEntity)
@JoinColumn({ name: 'user_id' })
user: Relation<UserEntity>;

Now you have:

member.userId       // FK value: 5
member.user         // UserEntity
member.user.id      // User's ID: 5

Notice I used userId, not user_id, because that's the normal TypeScript naming style.

Important distinction
member.userId
→ directly gets the FK value.

member.user
→ gets the related UserEntity when the relation is loaded.

member.user.id
→ gets the user's ID through the relation, but only if user was loaded.

So database column ≠ automatically available TypeScript property. @JoinColumn({ name: 'user_id' }) 
maps the database column to the user relation; it doesn't declare a user_id property.

--->
Common TypeORM Find Operators
import {
  In,
  Not,
  MoreThan,
  MoreThanOrEqual,
  LessThan,
  LessThanOrEqual,
  Between,
  Like,
  ILike,
  IsNull,
  IsNotNull,
} from 'typeorm'

| Operator            | Example                      | SQL meaning             |
| ------------------- | ---------------------------- | ----------------------- |
| `In()`              | `id: In([1, 2, 3])`          | `IN (1,2,3)`            |
| `Not()`             | `id: Not(5)`                 | `!= 5`                  |
| `MoreThan()`        | `id: MoreThan(5)`            | `> 5`                   |
| `MoreThanOrEqual()` | `id: MoreThanOrEqual(5)`     | `>= 5`                  |
| `LessThan()`        | `id: LessThan(5)`            | `< 5`                   |
| `LessThanOrEqual()` | `id: LessThanOrEqual(5)`     | `<= 5`                  |
| `Between()`         | `id: Between(5, 10)`         | `BETWEEN 5 AND 10`      |
| `Like()`            | `name: Like('%test%')`       | `LIKE '%test%'`         |
| `ILike()`           | `name: ILike('%test%')`      | case-insensitive `LIKE` |
| `IsNull()`          | `description: IsNull()`      | `IS NULL`               |
| `IsNotNull()`       | `description: Not(IsNull())` | `IS NOT NULL`           |

example:1
await repo.find({
  where: {
    id: In([1, 2, 3]),
  },
});

example:2
await this.workSpaceRepo.delete({
  id: In([1, 2, 3]),
}); 

example:3
await this.workSpaceRepo.delete({
  id: MoreThan(10),
});